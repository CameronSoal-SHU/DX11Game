#pragma once
#include "SimpleMath.h"
#include "WindowUtil.h"

namespace Settings {
	static DirectX::SimpleMath::Vector2 SCREEN_DIM { 1280, 720 };				// Size of window
	static DirectX::SimpleMath::Vector2 GAME_RES { 1920, 1080 };				// Rendering resolution
}